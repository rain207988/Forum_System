<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xzy.forum.dao.MessageMapper">
    <resultMap id="MessageDetailResultMap" type="com.xzy.forum.model.Message"
               extends="BaseResultMap">
        <association property="postUser" resultMap="com.xzy.forum.dao.UserMapper.BaseResultMap" columnPrefix="pu_"/>
        <association property="receiveUser" resultMap="com.xzy.forum.dao.UserMapper.BaseResultMap" columnPrefix="ru_"/>
    </resultMap>

    <sql id="MessageUserColumns">
        pu.id as pu_id,
        pu.nickname as pu_nickname,
        pu.avatarUrl as pu_avatarUrl,
        ru.id as ru_id,
        ru.nickname as ru_nickname,
        ru.avatarUrl as ru_avatarUrl,
        m.id,
        m.postUserId,
        m.receiveUserId,
        m.content,
        m.state,
        m.deleteState,
        m.createTime,
        m.updateTime
    </sql>

    <select id="selectInboxByReceiveUserId" parameterType="java.lang.Long" resultMap="MessageDetailResultMap">
        select
        <include refid="MessageUserColumns"/>
        from t_message m
        join t_user pu on m.postUserId = pu.id
        join t_user ru on m.receiveUserId = ru.id
        where m.receiveUserId = #{receiveUserId}
          and m.deleteState = 0
        order by m.createTime desc
    </select>

    <select id="selectDetailById" parameterType="java.lang.Long" resultMap="MessageDetailResultMap">
        select
        <include refid="MessageUserColumns"/>
        from t_message m
        join t_user pu on m.postUserId = pu.id
        join t_user ru on m.receiveUserId = ru.id
        where m.id = #{id}
          and m.deleteState = 0
    </select>
</mapper>
